# Changelog

## [1.1.0] - 2026-07-28

### Added

- `_hotspots.txt` に `ENVIRONMENT` セクションを追加。パッケージのバージョン、Unity の
  バージョン、生成日時、設定値を記録する。古い出力を後から読んだときに、どの版が
  作ったものか判別できるようにするため
- `_hotspots.txt` に `SELF-CHECK` セクションを追加。出力の妥当性を機械的に点検する。
  解析ツールのバグは「結果がそれらしく見える」ため気づきにくいので、疑わしい兆候を
  明示する。検査項目:
  - フレームが読めていない / `_markers.csv` が空（API 変更の疑い）
  - スパイクの内訳が上限で切り捨てられた
  - 要因を特定できなかったフレームがある（除外条件が広すぎる疑い）
  - 全フレームの要因が同一マーカー（除外漏れの疑い）
  - **リングバッファの巻き戻り検出**（下記）
  - 停止型 GC の検出 / `EditorLoop` 主因 / GPU 未記録
- `SUMMARY` に `duration`（実時間・秒）を追加。フレーム数だけでは何秒ぶんのデータか
  分からず、1349 フレームが実は 7.3 秒しかない、といった事実を見落とすため
- `SUMMARY` に `first frame` の判定を追加。Play 開始直後はシーンロードと JIT で必ず
  突出して重くなるため、先頭フレームが平凡なら「Profiler のリングバッファが巻き戻り、
  それ以前が失われている」と判断できる。調べたい現象が録画範囲の外にある場合に気づける

### Fixed

- **スパイクの内訳が欠落することがあった。** 内訳の対象フレームが上限120で、
  「重い順の埋め草」と枠を共有していたため、スパイクが120件を超えるキャプチャでは
  後半のスパイクが丸ごと記録されなかった（調査対象そのものが消える）。
  スパイクの上限を1000（実質無制限の安全弁）として分離し、埋め草は10フレームに限定。
  副次的に対象フレーム数が減り、解析時間も短くなった
- 上限到達の警告が常時出て意味をなしていなかったのを、実際に切り捨てが起きたときだけ
  `[ERROR]` を出すよう変更

### Documentation

- README に導入方法（git URL / ローカルパス参照 / フォルダコピー）を追加
- README に「記録範囲の制約」を追加。Profiler は直近2000フレームしか保持せず、
  240fps なら約11秒、60fps でも約33秒しか録れないこと、複数キャプチャを比較するときは
  記録範囲を揃える必要があることを明記
- README に「依存している不安定な API」を追加。`ProfilerDriver` が公開 API でないこと、
  過去に踏んだ非互換（`RawFrameDataView.threadCount` の不在、`ProfilerModule` の
  カウンタ0個拒否）を記録

## [1.0.0] - 2026-07-28

- 初版。Profiler の `.data` キャプチャを `_frames.csv` / `_markers.csv` /
  `_hotspots.txt` に書き出す
- Profiler ウィンドウに `Capture Dumper` モジュールを追加
- 待機マーカーを要因欄から除外し、停止型 GC を `gcMs` 列に分離
- ファイル経由の自動実行トリガー（CI / エージェント向け）
